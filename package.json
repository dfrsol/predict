{
  "name": "predict",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "lab -a code -t 100 -L -v",
    "db:start": "docker run -d -p 28015:28015 -p 8090:8080 -v $PWD/db:/data --name expertsdb rethinkdb",
    "db:stop": "docker stop expertsdb",
    "db:remove": "docker rm expertsdb",
    "test-cov-html": "lab -a code -L -r html -o coverage.html",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "good": "^7.0.1",
    "good-console": "^6.1.2",
    "good-squeeze": "^4.0.0",
    "hapi": "^14.2.0",
    "hoek": "^4.0.2"
  },
  "devDependencies": {
    "code": "^3.0.2",
    "eslint": "^3.3.1",
    "eslint-config-hapi": "^10.0.0",
    "eslint-plugin-hapi": "^4.0.0",
    "lab": "^11.0.0"
  }
}
